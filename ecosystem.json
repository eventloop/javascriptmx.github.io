{
  "apps" : [{
    "name"      : "WEB",
    "script"    : "server.js",
    "env": {
    },
    "env_production" : {
      "NODE_ENV": "production",
      "PORT": "3030"
    }
  }],
  "deploy" : {
    "production" : {
      "user" : "javascript",
      "host" : "javascriptmx.com",
      "ref"  : "origin/master",
      "repo" : "git@github.com:Siedrix/jsmexico.git",
      "path" : "/var/www/javascriptmx",
      "post-deploy" : "npm install --production && bower install --production && pm2 startOrRestart ecosystem.json --env production"
    }
  }
}
